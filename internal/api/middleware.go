package api

// TODO(E4): HTTP middleware for the standalone server: request logging,
// auth token, request ID, panic recovery. Re-use the message-side
// middleware from internal/broker where possible.
